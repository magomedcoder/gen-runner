services:
  gen:
    container_name: gen-server
    build:
      context: .
    command: /usr/local/bin/gen-server
    restart: always
    ports:
      - 50051:50051
    networks:
      - network-gen
    depends_on:
      - postgres

  postgres:
    container_name: postgres
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=gen
    volumes:
      - .docker-volumes/postgres/:/var/lib/postgresql/data
    networks:
      - network-gen

networks:
  network-gen:
    driver: bridge